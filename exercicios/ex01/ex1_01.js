// at 01

const prompt = require('prompt-sync')()

let n1 = Number(prompt("qual o numero?"))
let n2 = Number(prompt("qual o numero?"))

let valor = n1 + n2
console.log(`A soma entre ${n1} + ${n2} é igual: ${valor}`)